## Quality checklist

- [ ] Tests cover the changed behavior
- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] Environment-specific values and credentials are excluded
